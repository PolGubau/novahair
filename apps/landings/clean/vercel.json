{
	"buildCommand": "cd ../../.. && pnpm install && pnpm build:landing:clean",
	"outputDirectory": "dist",
	"installCommand": "pnpm install",
	"framework": "vite",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
