{
	"buildCommand": "cd ../../.. && pnpm install && pnpm build:landing:clean",
	"outputDirectory": "dist",
	"installCommand": "pnpm install",
	"framework": "vite",
	"ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"master\" ]] ; then git diff HEAD^ HEAD --quiet apps/landings/clean packages/ui packages/utils packages/client packages/booking-app; else exit 1; fi'",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
