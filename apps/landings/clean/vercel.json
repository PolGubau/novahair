{
	"buildCommand": "cd ../../.. && pnpm install && pnpm build:landing:clean",
	"outputDirectory": "dist",
	"installCommand": "pnpm install",
	"framework": "vite",
	"ignoreCommand": "bash -c 'git diff HEAD^ HEAD --quiet . ../../packages/ui ../../packages/utils ../../packages/client ../../packages/booking-app && exit 1 || exit 0'",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
